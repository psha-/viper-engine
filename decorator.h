#ifndef DECORATOR_H
#define DECORATOR_H

#include <component.h>


class Decorator : public Component
{
    public:
        Decorator();
        virtual ~Decorator();
    protected:
    private:
};

#endif // DECORATOR_H
