#include "snakecollider.h"

SnakeCollider::SnakeCollider()
{
    //ctor
}

bool SnakeCollider::CheckCollision(Collider* target)
{
    //for( auto it = m_segments.begin(); it != m_segments.end; ++it) {
    //    if( (*it).x == head.x && (*it).y == head.y ) {
    //        return true;
    //    }
    //}
}

SnakeCollider::~SnakeCollider()
{
    //dtor
}
